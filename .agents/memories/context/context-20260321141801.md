# Context Summary

- Added Rust build artifact ignore rule (`target/`) to prevent generated Cargo output from appearing as untracked files.
- This addresses feedback that `cargo run`/`cargo test` was generating noisy files not covered by `.gitignore`.
