- Added a canonical TypeScript endpoint inventory file under `rust/parity/typescript-endpoints.txt` for parity tracking.
- Extended `tests/run.js` to assert the inventory remains synchronized with endpoint URLs declared in `src/repositories`, `src/feeds`, and `src/services`.
- Updated endpoint parity checklist documentation to include endpoint inventory coverage in validation criteria.
