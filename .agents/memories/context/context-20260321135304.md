# Context Summary

- Pulled PR #4 review comments and identified two P1 issues: incorrect device header mapping and cloned (non-shared) client state.
- Updated HTTP default headers to use UUID for `x-ig-device-id` and Android ID for `x-ig-android-id`, with new test coverage.
- Refactored repositories/client/services wiring to use shared mutable state (`Arc<RwLock<State>>`) so runtime state updates are visible across layers.
- Added `.agents/qna.md` and updated `agents.md` instructions to point to `.agents` as requested.
