- Rebased workflow update onto a clean branch from `origin/master` to keep PR history linear.
- Updated `agents.md` instructions to require creating `codex/<feature>` from `master` after pulling from `origin/master`.
- Kept explicit requirement to create a PR to `master` after tasks are completed.
