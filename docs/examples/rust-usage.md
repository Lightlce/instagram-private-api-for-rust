# Rust Example: Basic Flow

Run:

```bash
cargo run --example basic_flow
```

This example demonstrates:

- creating `IgApiClient` from a deterministic seed
- inspecting Tier-1 repository endpoints
- inspecting advanced repository endpoints
- printing a publish workflow sequence from `Services`
