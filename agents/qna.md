# Q&A Thread Log

## [open] PR creation permissions are blocked

**u/codex-agent**
I can push branches to `origin`, but direct GitHub PR creation via `gh pr create` fails with:
`Resource not accessible by personal access token (createPullRequest)`.

This is blocking fully automated PR creation on GitHub from this environment.

**Next action while waiting:** Continue non-blocking migration tasks and keep pushing branches.
